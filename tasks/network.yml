- name: Ensure nginx network exists
  docker_network:
    name: "{{ nginx_network | default('nginx_network') }}"
    state: "{{ 'present' if nginx_install is true else 'absent' | default('absent') }}"
